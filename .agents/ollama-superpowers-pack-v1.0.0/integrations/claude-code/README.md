# Claude Code integration

```powershell
ollama launch claude --model gpt-oss:20b
ollama launch claude --model glm-5.2:cloud
```

Use the local model by default. Escalate only large, non-secret evidence packets.
