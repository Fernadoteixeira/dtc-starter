# Ollama Web Search through MCP

The official Ollama Python repository includes a Web Search MCP server example.
It requires `OLLAMA_API_KEY`. Keep the key in the environment, never in a committed
configuration file. Web Search and Web Fetch consume cloud/session allowance.
