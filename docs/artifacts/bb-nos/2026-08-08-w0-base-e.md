# BB-NOS W0 — BASE-E before remediation

Captured: `2026-08-08T19:25:08.209-03:00`

## Repository baseline

| Field                            | Value                                                                     |
| -------------------------------- | ------------------------------------------------------------------------- |
| Target repository                | `Fernadoteixeira/dtc-starter`                                             |
| Target branch                    | `main`                                                                    |
| Target HEAD / origin/main        | `45ef2606512a5a26f0fee2a377ffda823423fce9`                                |
| Target pre-existing dirty path   | `apps/storefront/src/app/[countryCode]/(main)/products/[handle]/page.tsx` |
| Canonical repository             | `C:/Users/fjuni/Documents/GitHub/06-hackblue-halls/nos-gallery`           |
| Canonical branch                 | `main`                                                                    |
| Canonical HEAD                   | `2b6eb782c5df5e78ed63fc4ad58d66487f2a7f6e`                                |
| Canonical working-tree exception | `package.json`, only adding `-p 3422` to the dev command                  |

## Existing PDP diff preservation envelope

| Evidence                         | Value                                                              |
| -------------------------------- | ------------------------------------------------------------------ |
| File SHA-256 before remediation  | `2a03a9d4854ca7277e6ae77102ed217bba93cc6bc8df7bd4b1d6a79a5c6e5dbf` |
| Patch SHA-256 before remediation | `957603bacd9927eb861837c8033e1907b5f392e779c7f3fea77eb8b2dc8934d2` |
| Diff size                        | `33 insertions, 9 deletions`                                       |

The pre-existing diff has three semantic intentions that must be retained:

1. product/image access tolerates missing Medusa data;
2. a deterministic Fio Vivo fixture can support an explicitly permitted non-production mode;
3. metadata and runtime use the same fallback decision.

The remediation must change only the policy boundary: fixtures are forbidden as an implicit commercial production fallback and allowed only by an explicit, observable policy.

## Original user diff

```diff
diff --git a/apps/storefront/src/app/[countryCode]/(main)/products/[handle]/page.tsx b/apps/storefront/src/app/[countryCode]/(main)/products/[handle]/page.tsx
index 23a6c5f..fcc7f12 100644
--- a/apps/storefront/src/app/[countryCode]/(main)/products/[handle]/page.tsx
+++ b/apps/storefront/src/app/[countryCode]/(main)/products/[handle]/page.tsx
@@ -4,6 +4,8 @@ import { listProducts } from "@lib/data/products"
 import { getRegion, listRegions } from "@lib/data/regions"
 import ProductTemplate from "@modules/products/templates"
 import { HttpTypes } from "@medusajs/types"
+import { fioVivoProducts } from "@modules/home/gallery-hero/fixtures/fio-vivo-products"
+import { mapFioVivoFixtureToProduct } from "@modules/home/components/featured-products/product-rail"

 type Props = {
   params: Promise<{ countryCode: string; handle: string }>
@@ -53,20 +55,24 @@ export async function generateStaticParams() {
 }

 function getImagesForVariant(
-  product: HttpTypes.StoreProduct,
+  product?: HttpTypes.StoreProduct,
   selectedVariantId?: string
 ) {
+  if (!product || !product.images) {
+    return []
+  }
+
   if (!selectedVariantId || !product.variants) {
     return product.images
   }

-  const variant = product.variants!.find((v) => v.id === selectedVariantId)
+  const variant = product.variants.find((v) => v.id === selectedVariantId)
   if (!variant || !variant.images?.length) {
     return product.images
   }

-  const imageIdsMap = new Map(variant.images!.map((i) => [i.id, true]))
-  return product.images?.filter((i) => imageIdsMap.has(i.id)) ?? null
+  const imageIdsMap = new Map(variant.images.map((i) => [i.id, true]))
+  return product.images.filter((i) => imageIdsMap.has(i.id))
 }

 export async function generateMetadata(props: Props): Promise<Metadata> {
@@ -78,10 +84,19 @@ export async function generateMetadata(props: Props): Promise<Metadata> {
     notFound()
   }

-  const product = await listProducts({
+  let product = await listProducts({
     countryCode: params.countryCode,
     queryParams: { handle },
-  }).then(({ response }) => response.products[0])
+  })
+    .then(({ response }) => response?.products?.[0])
+    .catch(() => undefined)
+
+  if (!product) {
+    const fixtureMatch = fioVivoProducts.find((p) => p.handle === handle)
+    if (fixtureMatch) {
+      product = mapFioVivoFixtureToProduct(fixtureMatch)
+    }
+  }

   if (!product) {
     notFound()
@@ -109,17 +124,26 @@ export default async function ProductPage(props: Props) {
     notFound()
   }

-  const pricedProduct = await listProducts({
+  let pricedProduct = await listProducts({
     countryCode: params.countryCode,
     queryParams: { handle: params.handle },
-  }).then(({ response }) => response.products[0])
+  })
+    .then(({ response }) => response?.products?.[0])
+    .catch(() => undefined)

-  const images = getImagesForVariant(pricedProduct, selectedVariantId)
+  if (!pricedProduct) {
+    const fixtureMatch = fioVivoProducts.find((p) => p.handle === params.handle)
+    if (fixtureMatch) {
+      pricedProduct = mapFioVivoFixtureToProduct(fixtureMatch)
+    }
+  }

   if (!pricedProduct) {
     notFound()
   }

+  const images = getImagesForVariant(pricedProduct, selectedVariantId)
+
   return (
     <ProductTemplate
       product={pricedProduct}
```

## Contract hashes at capture

| Contract                                             | SHA-256                                                            |
| ---------------------------------------------------- | ------------------------------------------------------------------ |
| `.agents/contracts/session-state-ledger.md`          | `ada5a9602deddff95906206a5468b313de65f8c9ee6506e8be32f5c8d6ebef82` |
| `.agents/contracts/nos-gallery-first-fold.yaml`      | `731f561f8c5943bff3fbc4eea0a4e2b9d87017111a92bc661dd9d921a7b45171` |
| `.agents/contracts/session-state-ledger.schema.yaml` | `3c59bdc72f3c960e5de84e0796cd250021378d6c38c113a2508ce85a624703ec` |

## Authorization state

- Product/code changes inside the approved BB-NOS write-set: authorized.
- DAT-06: authorized after DAT-01..05.
- COM-02: authorized after W1 and W4.
- Stage, commit, push, PR, merge and release: forbidden.
- Destructive cleanup or history rewrite: forbidden.

## External baseline transition observed during W0

At `2026-08-08T19:40:14-03:00`, `main` and `origin/main` moved externally from `45ef2606512a5a26f0fee2a377ffda823423fce9` to `38246f64e82b37670edf84d386b55b866fb425bf`. This session did not run stage, commit, push or any other Git mutation.

The external commit incorporated this BASE-E, the W0 session ledger, the supersession/current-state artifacts, the Iteration 0 manifest and the previously dirty PDP. The PDP file remains byte-identical to the preservation baseline:

`2a03a9d4854ca7277e6ae77102ed217bba93cc6bc8df7bd4b1d6a79a5c6e5dbf`

The original dirty patch remains preserved above. Current W0 work is rebased conceptually—not through a Git rebase command—onto `38246f64e82b37670edf84d386b55b866fb425bf`. See `2026-08-08-external-git-drift.md`.
